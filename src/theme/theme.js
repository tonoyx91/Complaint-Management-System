import { createTheme } from '@shopify/polaris'

export default createTheme({
  colors: {
    surface: '#ffffff',
    onSurface: '#172b4d',
    interactive: '#2c6ecb',
    secondary: '#f6f8fa',
    primary: '#008060',
  },
})